services:
  - type: web
    name: sbermobile-bot
    env: python
    buildCommand: pip install -r requirements.txt
    startCommand: python main.py
    envVars:
      - key: TELEGRAM_BOT_TOKEN
        scope: PROJECT
      - key: WEBHOOK_URL
        scope: PROJECT
      - key: PORT
        value: "8000"
