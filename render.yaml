services:
  - type: web
    name: sbermobile-bot
    env: python
    buildCommand: pip install -r requirements.txt
    startCommand: python main.py --polling
    envVars:
      - key: TELEGRAM_BOT_TOKEN
        value: ""
      - key: PORT
        value: "8000"
