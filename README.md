# SberMobile Telegram Support Bot

## Описание

🤖 Интеллектуальный чат-бот для поддержки клиентов SberMobile в Telegram. Бот автоматически ищет ответы на вопросы в базе знаний и предоставляет пользователям необходимую информацию.

## Возможности

✅ **Автоматический поиск ответов** - Бот находит релевантные ответы из FAQ SberMobile
✅ **Веб-хуки** - Использует веб-хуки для работы с Telegram (не polling)
✅ **Логирование** - Записывает все взаимодействия в SQLite БД
✅ **Категоризация** - Организует вопросы по категориям
✅ **Статистика** - Собирает данные о популярных вопросах
✅ **Бесплатное развертывание** - Совместимо с Render.com бесплатным планом

## Архитектура

```
sbermobile-bot/
├── app/
│   ├── __init__.py
│   ├── bot.py              # Основная логика бота
│   ├── handlers.py         # Обработчики команд
│   ├── faq_engine.py      # Поиск по FAQ
│   └── database.py        # Работа с БД
├── data/
│   ├── faq.json           # База FAQ (опционально)
│   └── bot.db             # SQLite база
├── main.py                # Точка входа
├── requirements.txt       # Зависимости
├── render.yaml           # Конфиг Render.com
├── .env.example          # Пример переменных окружения
├── .gitignore            # Git ignore
└── README.md             # Этот файл
```

## Требования

- Python 3.10+
- Telegram Bot API Token (получить можно у @BotFather)
- Render.com аккаунт (или другой хост с поддержкой Python)
- GitHub аккаунт

## Локальная установка

### 1. Клонировать репозиторий
```bash
git clone https://github.com/YOUR_USERNAME/sbermobile-bot.git
cd sbermobile-bot
```

### 2. Создать виртуальное окружение
```bash
python3 -m venv venv
source venv/bin/activate  # На Windows: venv\Scripts\activate
```

### 3. Установить зависимости
```bash
pip install -r requirements.txt
```

### 4. Настроить переменные окружения
```bash
cp .env.example .env
# Отредактировать .env и добавить токен
nano .env
```

### 5. Запустить локально (режим polling)
```bash
# Для локального тестирования используется polling
python main.py --polling
```

## Развертывание на Render.com

### 1. Создать GitHub репозиторий
```bash
git init
git add .
git commit -m "Initial commit: SberMobile Telegram Bot"
git branch -M main
git remote add origin https://github.com/YOUR_USERNAME/sbermobile-bot.git
git push -u origin main
```

### 2. Подключить к Render.com
1. Войти на https://render.com
2. Нажать "New +" → "Blueprint"
3. Выбрать репозиторий GitHub
4. Выбрать ветку `main`
5. Render.com автоматически прочитает `render.yaml`

### 3. Установить переменные окружения
В Render.com dashboard:
- `TELEGRAM_BOT_TOKEN` - получить у @BotFather в Telegram
- `WEBHOOK_URL` - URL вашего сервиса на Render (будет выглядеть как `https://sbermobile-bot.onrender.com`)

### 4. Развернуть
Нажать "Deploy" - Render.com автоматически:
- Создаст веб-сервер
- Установит зависимости из `requirements.txt`
- Запустит `python main.py`

## Команды бота

```
/start      - 🚀 Начать диалог
/help       - ❓ Справка по использованию
/categories - 📂 Показать категории FAQ
/contact    - ☎️ Контакты поддержки
```

## Обработка сообщений

### Поток взаимодействия:

```
Пользователь пишет вопрос
       ↓
Бот получает сообщение через веб-хук
       ↓
Нормализация текста
       ↓
Поиск в FAQ с использованием fuzzy matching (>50% совпадение)
       ↓
Если найдено:
  ✅ Отправить ответ с категорией и процентом совпадения
  📊 Залогировать в БД
Если не найдено:
  ❌ Предложить переформулировать или написать в поддержку
  📊 Залогировать в БД
```

## База FAQ

По умолчанию используется встроенная база с вопросами о:
- eSIM
- Услуги и пакеты
- Тарифы
- Перенос номера
- Подписка СберПрайм
- Мобильная связь и интернет
- Общая информация

### Обновление FAQ

Отредактировать `app/faq_engine.py` и обновить `DEFAULT_FAQ` или создать файл `data/faq.json`:

```json
[
  {
    "category": "eSIM",
    "questions": [
      {
        "question": "Как подключить eSIM?",
        "answer": "Инструкция здесь..."
      }
    ]
  }
]
```

## Логирование и аналитика

Все взаимодействия записываются в SQLite:

```sql
SELECT * FROM interactions;        -- Все взаимодействия
SELECT * FROM users;               -- Информация о пользователях
```

Статистика доступна в коде через функцию `get_stats()`:
- Количество вопросов
- Процент найденных ответов
- Топ вопросов
- Популярные категории

## Технические детали

### Веб-хуки vs Polling

Бот использует **веб-хуки** для продакшена (Render.com) и **polling** для локального тестирования.

**Веб-хуки:**
- Бот получает обновления через HTTPS POST запросы
- Работает на бесплатном плане Render.com
- Порт: 8000
- URL: `https://your-domain.onrender.com/webhook/{TOKEN}`

### Поиск по FAQ

Используется алгоритм `SequenceMatcher` из `difflib`:
- Вычисляет процент совпадения между вопросом пользователя и вопросами в БД
- Порог: 50% совпадение
- Выбирается вариант с максимальным совпадением

### Безопасность

✅ Нет платных функций
✅ Нет хранения чувствительных данных
✅ Использует только стандартные библиотеки Python
✅ SQLite вместо облачных БД

## Решение проблем

### "Webhook returns error code 401"
- Проверить, что `TELEGRAM_BOT_TOKEN` верный
- Проверить, что `WEBHOOK_URL` установлен корректно

### "Address already in use"
- Изменить PORT в переменных окружения
- Или убить процесс: `lsof -ti:8000 | xargs kill -9`

### Бот не отвечает
- Проверить логи в Render.com dashboard
- Убедиться, что bot.py корректно импортирует модули
- Проверить интернет соединение

### FAQ не загружается
- Проверить путь к `faq.json`
- Убедиться в валидности JSON
- Проверить права доступа к файлу

## Развитие проекта

### Идеи для улучшения:
- [ ] Интеграция с API SberMobile для реального времени
- [ ] Машинное обучение для лучшего поиска
- [ ] Многоязычная поддержка
- [ ] Интеграция с CRM системой
- [ ] Умные уведомления о новых вопросах
- [ ] Аналитическая панель
- [ ] Feedback система с рейтингом ответов

## Лицензия

MIT License - свободно используйте и модифицируйте

## Контакты

👨‍💻 Разработка: CX Research Team
📧 Email: support@sbermobile.local
💬 Telegram: @sbermobile_bot

## Благодарности

Спасибо:
- python-telegram-bot команде
- Render.com за бесплатное хостинг
- SberMobile за вдохновение

---

**Последнее обновление:** 2025-12-04
**Версия:** 1.0.0-beta
