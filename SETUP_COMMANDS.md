# ONE-LINER SETUP КОМАНДА ДЛЯ Mac

## Вариант 1: Вставить в терминал (рекомендуется)

```bash
# Скопируйте и вставьте ВСЮ эту строку в терминал Mac

mkdir -p sbermobile-bot && cd sbermobile-bot && \
mkdir -p app data && \
python3 -m venv venv && \
source venv/bin/activate && \
pip install --upgrade pip && \
pip install python-telegram-bot==20.5 aiohttp==3.9.1 httpx==0.25.1 python-dotenv==1.0.0 pydantic==2.5.0 && \
touch app/__init__.py main.py app/bot.py app/handlers.py app/faq_engine.py app/database.py && \
git init && \
git config user.email "bot@sbermobile.local" && \
git config user.name "SberMobile Bot Dev" && \
echo "✅ Структура создана! Теперь:" && \
echo "" && \
echo "1. Скопируй содержимое файлов из документации в соответствующие файлы" && \
echo "2. Запусти: python main.py --polling" && \
echo "3. Или: source venv/bin/activate && python main.py"
```

## Вариант 2: Пошагово (если вышло не ясно)

```bash
# 1. Создать папку и перейти в неё
mkdir -p sbermobile-bot
cd sbermobile-bot

# 2. Создать структуру
mkdir -p app data
touch app/__init__.py

# 3. Создать файлы (пусто на начало)
touch main.py app/bot.py app/handlers.py app/faq_engine.py app/database.py

# 4. Создать виртуальное окружение
python3 -m venv venv

# 5. Активировать его
source venv/bin/activate

# 6. Установить зависимости
pip install --upgrade pip
pip install python-telegram-bot==20.5
pip install aiohttp==3.9.1
pip install httpx==0.25.1
pip install python-dotenv==1.0.0
pip install pydantic==2.5.0

# 7. Инициализировать git
git init
git config user.email "bot@sbermobile.local"
git config user.name "SberMobile Bot Dev"

echo "✅ Готово! Структура создана"
```

## Вариант 3: Из готового скрипта (если скачал)

```bash
# Если у тебя уже есть setup_project.sh
bash setup_project.sh
```

---

# КОПИРОВАНИЕ ФАЙЛОВ - ИНСТРУКЦИЯ

После выполнения одной из команд выше у тебя будет структура:

```
sbermobile-bot/
├── app/
│   ├── __init__.py (пусто)
│   ├── bot.py (пусто)
│   ├── handlers.py (пусто)
│   ├── faq_engine.py (пусто)
│   └── database.py (пусто)
├── data/
├── main.py (пусто)
└── venv/ (виртуальное окружение)
```

## Копирование файлов (через VS Code или nano)

### В VS Code:

```bash
# Открыть проект в VS Code
code .
```

Затем для каждого файла:
1. Кликнуть на файл слева (например `main.py`)
2. Скопировать весь код из документации
3. Вставить в редактор
4. Ctrl+S (сохранить)

### Через nano (если нет VS Code):

```bash
# Для main.py
nano main.py
# Вставить код
# Ctrl+O → Enter → Ctrl+X

# Для app/bot.py
nano app/bot.py
# ... и так далее
```

---

# ФАЙЛЫ В ПОРЯДКЕ КОПИРОВАНИЯ

## 1️⃣ main.py
Скопируй весь код из раздела `main.py` документации

## 2️⃣ app/bot.py
Скопируй весь код из раздела `app/bot.py` документации

## 3️⃣ app/handlers.py
Скопируй весь код из раздела `app/handlers.py` документации

## 4️⃣ app/faq_engine.py
Скопируй весь код из раздела `app/faq_engine.py` документации

## 5️⃣ app/database.py
Скопируй весь код из раздела `app/database.py` документации

## 6️⃣ requirements.txt
Скопируй весь код из раздела `requirements.txt` документации

## 7️⃣ .env.example
Скопируй весь код из раздела `.env.example` документации

## 8️⃣ .gitignore
Скопируй весь код из раздела `.gitignore` документации

## 9️⃣ render.yaml
Скопируй весь код из раздела `render.yaml` документации

## 🔟 README.md
Скопируй весь код из раздела `README.md` документации

---

# ПЕРВЫЙ ЗАПУСК

## После копирования файлов:

```bash
# 1. Убедись что виртуальное окружение активировано
source venv/bin/activate

# 2. Создай .env файл
cp .env.example .env

# 3. Отредактируй .env
nano .env

# Заполни:
# TELEGRAM_BOT_TOKEN=ТВОЙ_ТОКЕН_ОТ_BOTFATHER
# WEBHOOK_URL=http://localhost:8000

# 4. Запусти бота
python main.py --polling
```

## Если всё ОК, увидишь:

```
2025-12-04 14:30:15 - bot - INFO - 🚀 Запуск SberMobile Support Bot...
2025-12-04 14:30:16 - bot - INFO - ✅ Бот успешно запущен!
2025-12-04 14:30:16 - bot - INFO - 💬 Бот готов к приему сообщений
```

## Тестирование в Telegram:

1. Открыть Telegram
2. Найти своего бота
3. Отправить `/start`
4. Попробовать вопрос: "Как подключить eSIM?"
5. Бот должен ответить!

---

# ВЫПУСК ДЛЯ GITHUB И RENDER.COM

## После успешного локального тестирования:

```bash
# Убедись что в проекте все файлы
ls -la

# Добавить all файлы в git
git add .

# Создать первый коммит
git commit -m "Initial commit: SberMobile Telegram Support Bot"

# Изменить название ветки на main
git branch -M main

# Создать репозиторий на GitHub (https://github.com/new)
# Скопировать команду и запустить:

git remote add origin https://github.com/YOUR_USERNAME/sbermobile-bot.git
git push -u origin main

# Готово! Теперь зайти на https://render.com и:
# 1. New → Blueprint
# 2. Выбрать репозиторий
# 3. Render прочитает render.yaml автоматически
# 4. Заполнить переменные окружения
# 5. Deploy!
```

---

# ПРОВЕРКА ОШИБОК

## Ошибка: "ModuleNotFoundError: No module named 'telegram'"

```bash
source venv/bin/activate
pip install python-telegram-bot==20.5
```

## Ошибка: "TELEGRAM_BOT_TOKEN not set"

```bash
# Проверить .env файл
cat .env

# Должно быть:
# TELEGRAM_BOT_TOKEN=твой_реальный_токен_здесь
```

## Ошибка: "Port 8000 already in use"

```bash
# Убить процесс
lsof -ti:8000 | xargs kill -9

# Или использовать другой порт
PORT=8001 python main.py --polling
```

## Ошибка: "Connection refused"

```bash
# Убедиться что виртуальное окружение активировано
source venv/bin/activate

# Проверить что все зависимости установлены
pip list

# Должны быть:
# python-telegram-bot
# aiohttp
# httpx
# python-dotenv
```

---

# ИТОГОВЫЙ ЧЕКЛИСТ

- [ ] Структура проекта создана (sbermobile-bot/app/data)
- [ ] Виртуальное окружение активировано (видно (venv) в начале строки)
- [ ] Зависимости установлены (pip list показывает telegram-bot)
- [ ] Все файлы Python скопированы и сохранены
- [ ] .env создан и заполнен токеном
- [ ] Бот запускается без ошибок (python main.py --polling)
- [ ] Бот отвечает на /start в Telegram
- [ ] Бот находит ответы на вопросы
- [ ] Git инициализирован (git init)
- [ ] GitHub репозиторий создан и код запушен
- [ ] Render.com развернут и работает

**Удачи! 🚀**
