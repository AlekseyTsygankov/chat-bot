# БЫСТРЫЙ СТАРТ - SberMobile Telegram Bot

## ⚡ За 5 минут до первого запуска

### Шаг 1: Скопировать все файлы проекта (2 мин)

```bash
# В терминале Mac создать новую папку
mkdir sbermobile-bot
cd sbermobile-bot

# Создать структуру
mkdir -p app data

# Создать пустые __init__.py файлы
touch app/__init__.py
touch app/bot.py
touch app/handlers.py
touch app/faq_engine.py
touch app/database.py
touch main.py
```

### Шаг 2: Скопировать содержимое файлов (2 мин)

Для каждого файла из документации:
1. Открыть файл (например `main.py`)
2. Скопировать весь код
3. Открыть в редакторе VS Code или nano
4. Вставить код

**Файлы для копирования (в этом порядке):**

1. **main.py** - точка входа приложения
2. **app/bot.py** - основная логика бота
3. **app/handlers.py** - обработчики команд
4. **app/faq_engine.py** - поиск по FAQ
5. **app/database.py** - работа с БД
6. **requirements.txt** - зависимости
7. **.env.example** - пример конфига
8. **.gitignore** - исключения для git
9. **render.yaml** - конфиг для Render.com
10. **README.md** - полная документация

### Шаг 3: Создать виртуальное окружение (1 мин)

```bash
# Из директории sbermobile-bot
python3 -m venv venv
source venv/bin/activate

# Проверить активацию (видно будет (venv) в начале строки)
```

### Шаг 4: Установить зависимости (1 мин)

```bash
pip install -r requirements.txt
```

### Шаг 5: Получить токен Telegram (1 мин)

1. Открыть Telegram и найти @BotFather
2. Отправить команду: `/newbot`
3. Следовать инструкциям (имя бота, юзернейм)
4. Скопировать полученный токен (выглядит как `123456789:ABCDEFGHIJKLMNOPQRSTUVWXYZabcdefg`)

### Шаг 6: Настроить .env

```bash
# Скопировать пример конфига
cp .env.example .env

# Отредактировать
nano .env
```

Заполнить:
```
TELEGRAM_BOT_TOKEN=ВСТАВЬТЕ_ТОКЕН_СЮДА
WEBHOOK_URL=http://localhost:8000
```

Сохранить: `Ctrl+O` → `Enter` → `Ctrl+X`

---

## 🚀 Локальное тестирование (режим Polling)

### Запуск бота локально

```bash
# Убедитесь, что виртуальное окружение активировано
source venv/bin/activate

# Запустить бота
python main.py --polling
```

Вывод должен быть похож на:
```
2025-12-04 14:30:15 - bot - INFO - 🚀 Запуск SberMobile Support Bot...
2025-12-04 14:30:16 - bot - INFO - ✅ Бот успешно запущен!
2025-12-04 14:30:16 - bot - INFO - 💬 Бот готов к приему сообщений
```

### Тестировать бота в Telegram

1. Открыть Telegram
2. Найти своего бота (по юзернейму)
3. Отправить `/start`
4. Тестировать команды и вопросы

---

## 📦 Подготовка к развертыванию на Render.com

### Шаг 1: Создать GitHub репозиторий

```bash
# Инициализировать git
git init
git config user.email "your@email.com"
git config user.name "Your Name"

# Добавить все файлы
git add .
git commit -m "Initial commit: SberMobile Telegram Bot"

# Изменить ветку (GitHub по умолчанию использует main)
git branch -M main
```

### Шаг 2: Загрузить на GitHub

1. Открыть https://github.com/new
2. Создать репозиторий `sbermobile-bot`
3. **НЕ** инициализировать README (уже есть)
4. Копировать команды для существующего репозитория

```bash
git remote add origin https://github.com/YOUR_USERNAME/sbermobile-bot.git
git push -u origin main
```

### Шаг 3: Развернуть на Render.com

1. Открыть https://render.com
2. Создать аккаунт (через GitHub проще)
3. Нажать **"New +"** → **"Blueprint"**
4. Выбрать GitHub репозиторий `sbermobile-bot`
5. Выбрать ветку `main`
6. Нажать **"Connect"**

Render.com прочитает `render.yaml` и предложит установить переменные:

#### Необходимые переменные:

| Переменная | Значение | Описание |
|------------|----------|---------|
| `TELEGRAM_BOT_TOKEN` | Токен от @BotFather | Необходимо получить |
| `WEBHOOK_URL` | `https://your-service.onrender.com` | Будет предложено Render.com |
| `PORT` | `8000` | Уже установлено в render.yaml |

7. Заполнить переменные
8. Нажать **"Deploy"**

Render.com будет:
- Клонировать репозиторий
- Установить зависимости из `requirements.txt`
- Запустить `python main.py`
- Развернуть веб-сервер с веб-хуками

---

## 🔧 Решение проблем

### Проблема: "ModuleNotFoundError: No module named 'telegram'"

**Решение:**
```bash
# Убедитесь, что виртуальное окружение активировано
source venv/bin/activate

# Переустановите зависимости
pip install --upgrade -r requirements.txt
```

### Проблема: "Бот не отвечает в Telegram"

**Проверить:**
1. Правильно ли скопирован `TELEGRAM_BOT_TOKEN`?
2. Запущен ли скрипт (`python main.py --polling`)?
3. Нет ли ошибок в консоли?

**Если локально работает:**
- Проблема может быть в Render.com развертывании
- Проверить логи в Render.com dashboard

### Проблема: "Port 8000 already in use"

**Решение:**
```bash
# Найти процесс, использующий порт
lsof -ti:8000

# Убить процесс (замените PID на число из предыдущей команды)
kill -9 <PID>

# Или просто использовать другой порт
PORT=8001 python main.py --polling
```

### Проблема: "FAQ не находит ответы"

**Причины:**
1. Вопрос слишком отличается от примеров в FAQ
2. Низкий порог совпадения (50% по умолчанию)

**Решение:**
- Отредактировать вопросы в `app/faq_engine.py` в `DEFAULT_FAQ`
- Или создать файл `data/faq.json` с вашей базой

---

## 📊 Проверка статистики

### Просмотр логов БД

```bash
# Активировать Python интерпретатор
python3

# В интерпретаторе:
from app.database import get_stats
stats = get_stats()
print(stats)
```

Вывод:
```python
{
    'total_queries': 42,
    'found_answers': 38,
    'unique_users': 15,
    'success_rate': 90.5,
    'top_questions': [...],
    'top_categories': [...]
}
```

---

## 🎯 Следующие шаги

После успешного развертывания:

1. **Расширить FAQ** - добавить реальные вопросы/ответы SberMobile
2. **Интегрировать API** - подключить реальные данные SberMobile
3. **Улучшить поиск** - использовать ML для лучшего matching
4. **Аналитика** - создать панель статистики
5. **Мониторинг** - настроить алерты об ошибках

---

## 📞 Поддержка

- **Документация**: Смотреть README.md
- **Примеры**: Вопросы в `app/faq_engine.py` → `DEFAULT_FAQ`
- **Логи**: В Render.com dashboard

---

**Версия:** 1.0.0-beta
**Дата:** 2025-12-04
